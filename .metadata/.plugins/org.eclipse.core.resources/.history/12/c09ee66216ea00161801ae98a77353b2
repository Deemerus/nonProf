import java.io.File;
import java.io.FileNotFoundException;
import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;

public class App {

	public static void main(String[] args) {
		Map<Integer, Neighbours> nodes = new HashMap<Integer, Neighbours>();
		try(Scanner scanner = new Scanner(new File("graph2"))){
			while(scanner.hasNextLine()){
				nodes.put(scanner.nextInt()*1000+scanner.nextInt(), new Neighbours());
				
			}
		} catch (FileNotFoundException e) {
			System.out.println("File graph2 not found.");
		}

	}
}
